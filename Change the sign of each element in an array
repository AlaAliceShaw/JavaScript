question
Write a function called oppositeArray that takes an array arr as an argument and returns an array in which each element has been 
replaced with the opposite element.

solution:

function oppositeArray(arr){
  let newArr = [];
   for(let i = 0; i < arr.length; i++){
   newArr.push(arr[i]*(-1))
  }
return newArr;
}
