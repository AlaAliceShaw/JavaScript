question
Write a function countOfSafeNumbers that takes an arr array as an argument and returns the number of elements in the array,
which are computational safe numbers.

solution:

function countOfSafeNumbers(arr){
  let count = 0;
    for(let i = 0; i < arr.length; i++){
      if(Number.isSafeInteger(arr[i])=== true)
    count++
    }
    return count;
 }
