Напишите функцию с именем calculate, которая принимает два числа a и b и строку operation (знак арифметической операции) в качестве аргументов,
выполняет данную операцию над числами и возвращает результат в виде строки, содержащей решение. 
Возможные знаки операций: '+', '-', '*', '/', '%'. Результат деления чисел округлить до ближайшего целого числа с помощью метода Math.round().
Гарантируется, что во всех тестах будут использоваться только данные арифметические операции.
В решении необходимо использовать оператор switch.

solution:
function calculate(a, b, operation){
switch(operation) {
case "+": return `${a} + ${b} = ${a+b}`;
case "-": return `${a} - ${b} = ${a-b}`;
case "*": return `${a} * ${b} = ${a*b}`;
case "%": return `${a} % ${b} = ${a%b}`;
case "/": return `${a} / ${b} = ${Math.round(a/b)}`;
}
}
