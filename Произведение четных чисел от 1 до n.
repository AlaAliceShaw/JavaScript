Найдите произведение четных чисел от 2 до n, т.е. 2 * 4 * 6 * ...
Напишите функцию с именем multOfEven, которая принимает число n в качестве аргумента и возвращает произведение четных чисел от 2 до n.
При решении используйте цикл while.
Примеры:
функция multOfEven(2) должна возвратить 2;
функция multOfEven(6) должна возвратить 48;
функция multOfEven(10) должна возвратить 3840.

solution:
function multOfEven(n){
let mult = 1;
let x = 1;
while(x <= n){
if(x % 2 === 0)
mult = mult * x;
x++;
}
return mult;
}
